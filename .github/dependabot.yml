# Dependabot: aktualisiert die Abhängigkeiten 
# und schickt Pull Requests nach Security Fixes
# WICHTING: Limits auf Pull Requests zu setzen!!!

version: 2
updates:
      # Frontend
    - package-ecosystem: 'npm'
      directory: '/frontend'
      schedule:
          interval: 'weekly'
      on:
      pull_request:
        paths:
          - frontend/**
      open-pull-request-limit: 1
      labels: 
        - 'dependencies'
        - 'security'
      groups:
        frontend-deps: 
          patterns:
            - "*"
      # Backend
    - package-ecosystem: 'npm'
      directory: '/backend'
      schedule:
          interval: 'weekly'
      on:
        pull_request:
          paths:
            - backend/**
      open-pull-request-limit: 1
      labels: 
        - 'dependencies'
        - 'security'
      groups:
        backend-deps: 
          patterns:
            - "*"
      # GitHub Actions
    - package-ecosystem: 'github-actions'
      directory: "/"
      schedule:
        interval: "weekly"
      on:
        pull_request:
          paths:
            - .github/workflows/**
      open-pull-request-limit: 1
      labels:
        - 'dependencies'
        - 'ci'
      groups:
        ci-deps: 
          patterns:
            - "*"
